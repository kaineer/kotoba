#
#
#
#
#

task :default do
  ruby "main.rb"
end

namespace :db do
  task :scheme do
    require 'models'
  end

  task :reset => :scheme do
    Models.reset
  end

  task :migrate => :scheme do
    Models.migrate
  end
end
